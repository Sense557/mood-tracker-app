import React from 'react';
import { View, Text } from 'react-native';

const HomeScreen: React.FC = () => {
  return (
    <View>
      <Text>Welcome to the Mood Tracker App!</Text>
    </View>
  );
};

export default HomeScreen; // Make sure you are using `export default`
